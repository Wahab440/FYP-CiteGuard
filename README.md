# FYP-CiteGuard

Repository layout:

- `UI/` contains the React + Vite application.
- The repository root contains project documentation and diagrams.

Run the UI from the app folder:

```powershell
cd UI
npm install
npm run dev
```